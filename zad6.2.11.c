#include <stdio.h>
#include <stdlib.h>
int suma(int tab [][], int a)
{
    int wynik;
    for(int i = 0, i<a; i++)
    {
        for(int j= 0 j<100; j++)
        {
            wynik+= tab [i][j]
        }
    }

    return wynik;
}
